<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluemoon.backend.mapper.HoldingMapper">

    <resultMap id="HoldingResultMap" type="com.bluemoon.backend.domain.holding.Holding">
        <id column="id" property="id"/>
        <result column="account_id" property="accountId"/>
        <result column="stock_code" property="stockCode"/>
        <result column="quantity" property="quantity"/>
        <result column="avg_price" property="avgPrice" javaType="java.math.BigDecimal"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByAccountIdAndQuantityGreaterThan" resultMap="HoldingResultMap">
        SELECT id, account_id, stock_code, quantity, avg_price, updated_at
        FROM holdings
        WHERE account_id = #{accountId}
          AND quantity > #{quantity}
    </select>

    <!-- ACCOUNTS 락을 먼저 획득한 뒤에만 호출할 것 (락 획득 순서: ACCOUNTS -> HOLDINGS, CLAUDE.md 절대 규칙) -->
    <select id="findByAccountIdAndStockCodeForUpdate" resultMap="HoldingResultMap">
        SELECT id, account_id, stock_code, quantity, avg_price, updated_at
        FROM holdings
        WHERE account_id = #{accountId}
          AND stock_code = #{stockCode}
        FOR UPDATE
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO holdings (account_id, stock_code, quantity, avg_price, updated_at)
        VALUES (#{accountId}, #{stockCode}, #{quantity}, #{avgPrice}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE holdings
        SET quantity   = #{quantity},
            avg_price  = #{avgPrice},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

</mapper>
