# 오라클 클라우드 인스턴스 생성 & 네트워크 설정 가이드

포트폴리오 백엔드(Spring Boot)를 오라클 클라우드 Always Free 인스턴스에 배포하기 위한 과정 정리.

---

## 1. 계정 준비

- oracle.com에서 Always Free 계정 가입 (신용카드 등록 필요하나 평생 무료 티어 존재)
- 30일 체험판(트라이얼) + $300 크레딧이 함께 제공되지만, **Always Free 대상 리소스는 트라이얼이 끝나도 계속 무료**
- **자동 결제 없음**: "Pay As You Go"로 직접 업그레이드하지 않는 한 카드에서 돈이 빠져나가지 않음
- 계정 유형 확인: 콘솔 > Billing & Cost Management > Manage Subscriptions 에서 확인 가능

### 가입 시 자주 겪는 문제
| 증상 | 원인/해결 |
|---|---|
| "등록을 완료할 수 없습니다" 에러 | 체크카드보다 신용카드 시도, 새 이메일로 재시도, 주소 형식 정확히 입력 |
| 카드 승인 후 계속 취소 반복 | 오라클 사기방지 시스템이 카드/계정을 의심 처리한 상태. 다른 환경(IP·브라우저)에서 재시도하거나 다른 카드 사용 |
| 리전을 바꿔도 동일 | 가입 오류는 리전과 무관한 경우가 많음 |

---

## 2. 인스턴스(VM) 생성

### Shape(사양) 선택
- **VM.Standard.E2.1.Micro**: x86, 1 OCPU / 1GB — 재고 여유로운 편, 가벼운 프로젝트에 적합. **이 프로젝트는 이 스펙 기준으로 설계됨** (`docker-compose.prod.yml`의 `mem_limit`, JVM 힙 설정 전부 1GB 기준)
- **VM.Standard.A1.Flex**: ARM, 최대 **2 OCPU / 12GB**까지 무료 (2026년 기준. 과거엔 4 OCPU/24GB였으나 축소됨)
  - **Always Free-eligible** 라벨이 붙은 shape만 무료
  - 한도 초과 시 초과분만 종량제 과금 (전체가 유료 전환되는 게 아님)
  - **재고 부족("Out of capacity") 에러가 매우 흔함** — 특히 서울/오사카/도쿄 등 아시아 리전
  - Always Free 인스턴스는 반드시 **홈 리전(가입 시 정해진 리전)에서만** 생성 가능. 다른 리전 구독 추가해도 무료 적용 안 됨
  - ARM을 쓰면 `docker-compose.prod.yml`의 `mem_limit`을 늘려도 되지만, 이미지들이 멀티아치(`amd64`/`arm64`)를 지원하는지 먼저 확인할 것 (`redis:7-alpine`, `nginx:1.27-alpine`, `eclipse-temurin:17-jre-alpine`, `gradle:8.10-jdk17-alpine` 전부 멀티아치 지원이라 문제 없음)

### 이미지(OS) 선택
- **Ubuntu**로 선택 (이 가이드의 4번 항목이 Ubuntu 기준, `apt` 사용)
- ARM(A1.Flex) 셰이프 선택 시 이미지도 반드시 **ARM(aarch64)용**을 선택해야 함

### "Estimated Cost" 위젯 주의
- 이 화면에 표시되는 예상 비용은 **Always Free 할인을 반영하지 않는 정가 표시**임 (알려진 UI 특성)
- Always Free-eligible 라벨이 붙어 있다면 이 숫자는 무시해도 됨
- 실제 청구 여부는 인스턴스 생성 후 **Billing > Cost Analysis**에서 확인

### 재고 부족 에러 대응
```
API Error: Out of capacity for shape VM.Standard.A1.Flex in availability domain AD-1
```
- 스펙을 낮춰서 재시도 (예: 1 OCPU/4GB)
- 시간대를 바꿔서 재시도 (새벽 시간대가 상대적으로 유리하다는 보고 많음)
- 짧은 간격으로 반복 시도 시 `Too many requests` 에러 발생 가능 → 5~10분 이상 간격 두고 재시도
- 근본적으로 안 풀리면 E2.1.Micro로 우회하거나 다른 클라우드(Hetzner, DigitalOcean 등) 병행 고려

---

## 3. 네트워킹 설정 (가장 헷갈리기 쉬운 부분)

### 인스턴스 생성 화면에서 흔히 겪는 문제
1. **VCN/Subnet 라디오 버튼이 실제로 선택 안 된 상태로 진행됨**
   - "Create new virtual cloud network", "Create new public subnet" 텍스트만 보고 넘어가면 안 되고, 라디오 버튼 자체를 클릭해서 안에 점(●)이 채워졌는지 확인해야 함
2. **Public IPv4 address assignment 체크박스가 계속 회색(비활성)**
   - Private Subnet이 선택된 상태이거나, 인라인 VCN 생성 과정에서 설정 누락이 생긴 경우
   - **가장 안정적인 해결책**: 인스턴스 생성 화면 안에서 만들지 말고, `Networking > Virtual Cloud Networks > Start VCN Wizard > Create VCN with Internet Connectivity`로 미리 VCN을 만들어두고, 인스턴스 생성 시 "Select existing virtual cloud network"로 선택

### 인스턴스를 이미 만들었는데 Public IP가 없는 경우 (사후 조치)
Internet Gateway 자체가 서브넷에 연결 안 된 것이 원인인 경우가 많음. 아래 순서로 해결:

1. 인스턴스 상세 페이지 > **Networking** 탭 이동
2. 화면 하단 **Quick Actions > "Connect public subnet to internet"** 클릭
   - Internet Gateway, Network Security Group, Route Table을 자동으로 구성해줌
   - 이미 Internet Gateway가 있다면 기존 것을 재사용
3. 생성된 **Network Security Group**으로 이동해 **Ingress 규칙 추가** (기본적으로 Egress만 열려있고 Ingress는 비어있음)
   - SSH: TCP 22, Source `0.0.0.0/0`
   - HTTP: TCP 80, Source `0.0.0.0/0`
   - HTTPS: TCP 443, Source `0.0.0.0/0` (HTTPS 붙일 계획이면 미리 열어두기)
4. **Primary VNIC > IPv4 Addresses > 대상 Private IP 옆 메뉴(⋮) > Edit**
5. **Public IP type**을 **"Ephemeral public IP"**로 선택 (Reserved는 유료) → Update
6. 인스턴스 상세 페이지에서 Public IP Address가 표시되는지 확인

> 이 NSG/Security List는 오라클 콘솔 쪽 방화벽이고, 아래 4-4에서 여는 `iptables`는 VM 내부 방화벽이다. **둘 다** 열어야 실제로 포트가 열린다.

---

## 4. 서버 접속 및 배포 (이 프로젝트는 Docker Compose로 배포)

이 프로젝트는 백엔드를 통째로 jar로 뽑아 systemd로 돌리는 방식이 아니라, **`docker-compose.prod.yml`로 redis + backend + nginx 세 컨테이너를 한 번에 띄우는 방식**이다. JDK/Gradle도 서버에 따로 설치할 필요 없음 — `backend/Dockerfile`이 멀티스테이지 빌드라 컨테이너 안에서 전부 해결된다(빌드 스테이지: `gradle:8.10-jdk17-alpine`, 실행 스테이지: `eclipse-temurin:17-jre-alpine`).

### 4-1. SSH 접속
```bash
chmod 400 키파일.key
ssh -i 키파일.key ubuntu@[Public_IP]
```
- 개인키(.pem/.key) 파일은 인스턴스 생성 시 **한 번만 다운로드 가능**(재확인 불가), 반드시 저장해둘 것
- Windows는 PowerShell에 OpenSSH가 기본 내장되어 있어 별도 설치(PuTTY 등) 없이 동일한 명령어 사용 가능

### 4-2. Docker 설치 (JDK는 설치할 필요 없음)
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install docker.io docker-compose -y
sudo systemctl enable docker && sudo systemctl start docker
sudo usermod -aG docker ubuntu   # sudo 없이 docker 쓰려면 (적용하려면 재접속 또는 newgrp docker)
```
> `apt`로 설치하면 구버전 `docker-compose`(하이픈, python 기반)가 깔린다. 최신 `docker compose`(띄어쓰기, 플러그인) 명령이 없으면 아래 모든 `docker compose ...` 명령을 `docker-compose ...`로 바꿔서 실행하면 된다. 어느 쪽이 설치됐는지 확인:
> ```bash
> docker compose version 2>/dev/null || docker-compose version
> ```

### 4-3. 스왑 추가 (1GB RAM에서 이미지 빌드하려면 사실상 필수)
`docker compose --build`가 컨테이너 안에서 Gradle 빌드를 돌리는데, 1GB RAM만으로는 빌드 중 OOM으로 죽을 수 있다.
```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile swap swap defaults 0 0' | sudo tee -a /etc/fstab
free -h   # 확인
```

### 4-4. OS 자체 방화벽(iptables) 포트 열기
오라클 콘솔에서 NSG/Security List를 열어도 OS 자체 방화벽은 별도로 열어야 함:
```bash
sudo iptables -I INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -I INPUT -p tcp --dport 443 -j ACCEPT   # HTTPS 붙일 계획이면 미리
sudo apt install -y iptables-persistent   # 재부팅 후에도 규칙 유지
sudo netfilter-persistent save
```

### 4-5. 프로젝트 코드 받기
```bash
# private 리포면 URL에 개인 액세스 토큰 포함: https://<TOKEN>@github.com/계정/AI_WTS.git
# 또는 서버에서 ssh-keygen으로 키를 만들어 GitHub 저장소의 Deploy key로 등록
git clone <이 저장소 URL> AI_WTS
cd AI_WTS
```

### 4-6. 오라클 Autonomous Database(Always Free) 생성 및 연결
컴퓨트 인스턴스와 별개의 무료 리소스라 컴퓨트 스펙과 상관없이 이용 가능. **지갑(wallet) 파일 없이 TLS로만 붙는 방식**으로 진행 (별도 볼륨 마운트 불필요).

1. OCI 콘솔 ☰ 메뉴 → **Oracle AI Database → Autonomous AI Databases → Create Autonomous Database**
   (예전엔 "Oracle Database → Autonomous Database"였는데, 오라클이 Database 23ai 홍보하면서 메뉴명만 "AI"가 붙은 걸로 바뀜 — 같은 서비스, Always Free 옵션도 그대로 있음)
2. Workload type: **Transaction Processing**, Deployment type: **Serverless**
3. **"Always Free"** 토글 반드시 ON (꺼져 있으면 과금 대상)
4. Administrator credentials: 사용자명은 고정 `ADMIN`, 비밀번호는 직접 지정(대/소문자+숫자 포함 12자 이상) — **꼭 메모**
5. Choose network access: **"Secure access from allowed IPs and VCNs only"** 선택
   - **"Secure access from everywhere"를 선택하면 mTLS 체크박스가 회색으로 비활성화된 채 강제로 켜져 있다.** ACL(Access Control List)이 하나 이상 등록돼 있어야만 mTLS를 끌 수 있는 구조라, "everywhere"로는 애초에 해제가 불가능함
6. IP value 입력란에 ACL 등록:
   - 배포용 컴퓨트 인스턴스(2번 항목에서 만든 VM)의 **퍼블릭 IP**를 추가 (백엔드가 실제로 이 DB에 접속하는 주체이므로 필수)
   - 로컬 PC에서 SQL Developer나 OCI Database Actions로 직접 DB를 조회하고 싶다면, **"Add my IP address"** 버튼을 눌러 지금 콘솔에 접속 중인 내 PC의 **공인(public) IP**도 함께 등록 (ipconfig로 나오는 `192.168.x.x` 같은 사설 IP를 직접 입력하는 게 아니라, 콘솔이 자동 감지해주는 값을 그대로 씀 — 사설 IP는 인터넷에서 보이지 않아 ACL에 넣어도 무의미함)
   - 가정용 인터넷은 대부분 유동 IP라 나중에 바뀌면 접속이 막힐 수 있음 → 그때 다시 콘솔에서 "Add my IP address"로 갱신
   - IP를 아직 모르거나 인스턴스를 나중에 만들 예정이면 임시로 `0.0.0.0/0`을 넣어 우선 mTLS만 풀고, 인스턴스 생성 후 **Edit → Access Control List**에서 실제 IP(`/32`)로 좁힐 것 — `0.0.0.0/0`을 그대로 두면 사실상 "everywhere" 허용과 같아 ACL을 설정하는 의미가 없어짐
7. ACL이 하나 이상 등록되면 그 아래 **"Require mutual TLS (mTLS) authentication" 체크 해제** — 이게 핵심. 켜두면 지갑 파일이 필수가 되고, 끄면 지갑 없이 TLS만으로 연결 가능
8. **Create Autonomous Database** → 몇 분 뒤 상태가 `ACTIVE`로 바뀜
9. 생성된 DB 클릭 → **Database Connection** 버튼 → 인증 방식이 **TLS**(Mutual TLS 아님)인지 확인 → Connection Strings 목록에서 **`<DB이름>_high`** 복사 (다음과 같은 형태):
   ```
   (description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.ap-chuncheon-1.oraclecloud.com))(connect_data=(service_name=abc123def456_mydb_high.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
   ```

### 4-7. `.env.prod` 작성

두 가지 방법 중 편한 쪽으로 진행.

**방법 A. 서버에서 직접 작성**
```bash
cp .env.prod.example .env.prod
nano .env.prod
```
채울 값:
```
SPRING_DATASOURCE_URL=jdbc:oracle:thin:@(description=...위에서 복사한 문자열 그대로...)
SPRING_DATASOURCE_USERNAME=ADMIN
SPRING_DATASOURCE_PASSWORD=<4-6에서 정한 비밀번호>

JWT_SECRET=<openssl rand -base64 48 로 생성한 랜덤 문자열>
KIS_APP_KEY=
KIS_APP_SECRET=
KIS_ACCOUNT_NO=
OPENAI_API_KEY=
NEWSDATA_API_KEY=
```
지갑 없는 방식이라 `TNS_ADMIN`/`secrets/wallet` 볼륨 관련 항목은 신경 쓸 필요 없음(`.env.prod.example` 상단 주석에 지갑 방식 설명도 같이 있으니 참고).

**방법 B. 로컬에서 미리 작성 후 scp로 전송**

`.env.prod`는 `.gitignore`에 걸려 있어서 git push로는 서버에 안 올라간다. 로컬(Windows)에서 값을 다 채운 `.env.prod`가 이미 있다면, SSH 접속에 쓰던 개인키를 그대로 써서 PowerShell에서 `scp`로 전송하면 된다(Windows에 OpenSSH가 기본 내장돼 있어 별도 설치 불필요):
```powershell
scp -i "키파일.key" ".env.prod의 로컬 경로" ubuntu@<Public_IP>:~/AI_WTS/.env.prod
```
- 목적지 경로는 4-5에서 `git clone`한 디렉터리(`~/AI_WTS`) 기준. 실제 경로가 다르면 서버에서 `pwd`로 확인 후 맞게 수정
- 전송 후 서버에서 확인 및 권한 제한:
  ```bash
  cat ~/AI_WTS/.env.prod          # 값이 제대로 들어왔는지 확인
  chmod 600 ~/AI_WTS/.env.prod    # 소유자(ubuntu)만 읽기/쓰기 가능하도록 제한
  ```
- GUI를 선호하면 WinSCP 같은 SFTP 클라이언트에 동일 키 파일을 등록해서 드래그 앤 드롭으로 옮겨도 됨

### 4-8. Docker Compose로 빌드 & 실행
```bash
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
```
- `docker-compose.yml`(로컬 개발용, Oracle DB 컨테이너 포함)이 아니라 **`docker-compose.prod.yml`**을 써야 함 — Oracle DB 컨테이너 없이 위에서 만든 Autonomous DB에 연결하고, `redis`/`backend`/`nginx` 각각에 `mem_limit`을 나눠 걸어 1GB 안에서 돌아가게 되어 있음
- 처음 빌드라 Gradle 의존성 다운로드 때문에 몇 분 걸림

### 4-9. 동작 확인
```bash
docker compose -f docker-compose.prod.yml ps                # 3개 컨테이너 모두 running/healthy 인지
docker compose -f docker-compose.prod.yml logs -f backend    # Flyway 마이그레이션이 끝까지 올라가는지 확인
curl localhost/health                                         # nginx -> backend actuator health, {"status":"UP"} 나오면 정상
```
외부에서(로컬 브라우저 등) `http://<Public_IP>/health`도 열려야 함 — 안 되면 3번(NSG/Security List)이나 4-4(iptables)를 다시 확인.

### 4-10. 프론트엔드(Vercel) 연결
Vercel 프로젝트 → Settings → Environment Variables:
```
NEXT_PUBLIC_API_BASE_URL=http://<Public_IP>
```
저장 후 재배포. 백엔드 CORS는 이미 전체 origin 허용(`SecurityConfig`의 `allowedOriginPatterns: *`)으로 설정돼 있어 추가 수정 없이 바로 연결됨.

### 4-11. HTTPS (선택, 도메인이 있을 때)
nginx가 호스트가 아니라 **컨테이너 안에서** 도는 구조라, 호스트에서 바로 `certbot --nginx`를 돌리는 방식은 안 맞는다. 아래 중 하나를 선택:
- **가장 간단한 방법**: 호스트에 `certbot`을 설치해 `--standalone` 모드로 80번 포트를 잠깐 점유시켜 인증서만 발급받고(`sudo docker compose -f docker-compose.prod.yml stop nginx` 후 진행), 발급된 인증서 경로(`/etc/letsencrypt/live/도메인/`)를 `docker-compose.prod.yml`의 `nginx` 서비스에 볼륨으로 마운트한 뒤 `nginx/nginx.conf`에 `listen 443 ssl;` 블록과 `ssl_certificate`/`ssl_certificate_key` 설정을 추가
- 갱신은 `certbot renew`를 cron에 등록하고, 갱신될 때마다 `docker compose -f docker-compose.prod.yml restart nginx` 필요
- 처음엔 굳이 안 해도 됨 — `http://Public_IP`로도 시연/과제 제출용으로는 충분

### 재부팅 후에도 계속 떠 있는지
`docker-compose.prod.yml`의 각 서비스에 `restart: unless-stopped`가 걸려 있고, `sudo systemctl enable docker`로 Docker 데몬 자체가 부팅 시 자동 실행되도록 해뒀으므로, VM이 재부팅돼도 컨테이너들이 자동으로 다시 올라온다. 별도 systemd 유닛을 직접 만들 필요는 없음.

---

## 5. 참고 — 자원 요구량 (Spring Boot + MyBatis + Redis, RAG 없음)

이 프로젝트는 원래 AI 인사이트를 별도 Python RAG 서비스(FastAPI+LangChain+Chroma)로 분리하려 했으나, 1GB VM에 실제로 올려보니 Python 프로세스까지 함께 뜨기엔 메모리가 부족해 Spring Boot 배치 내부로 통합했다(`archive/rag-service`에 백업). 그래서 지금 배포 대상은 벡터 검색/임베딩 없이 **Spring Boot(백엔드) + Redis + Nginx**뿐이다.

| 컨테이너 | `mem_limit` | 비고 |
|---|---|---|
| `redis` | 80MB | JWT/시세 캐시 |
| `backend` | 550MB | `JDK_JAVA_OPTIONS: -XX:MaxRAMPercentage=50.0 -XX:+UseSerialGC`로 힙을 275MB 정도로 제한, 1 OCPU 환경에 맞춰 SerialGC 사용 |
| `nginx` | 32MB | 리버스 프록시만 |

합쳐도 1GB 안쪽으로 들어오게 설계됨. E2.1.Micro(1 OCPU/1GB)로 충분하고, A1.Flex(2 OCPU/12GB 등)를 쓰면 여유가 훨씬 많아지지만 `docker-compose.prod.yml`의 `mem_limit`을 그대로 둬도 동작에는 문제 없음(더 널널해질 뿐).

> DB(Oracle Autonomous Database, Always Free)는 컴퓨트 인스턴스와 별개의 무료 자원이라 위 표에 카운트되지 않음.

---

## 6. 대안 클라우드 (오라클 재고 부족 시)

| 제공사 | 대략적 스펙/가격 | 특징 |
|---|---|---|
| Hetzner Cloud | 2~4 vCPU / 4~8GB, 월 $5~14 | 가격 대비 스펙 최고, 한국 기준 레이턴시 있음(싱가포르 리전), 한글 미지원 |
| DigitalOcean | 1 vCPU / 2GB, 월 $12 | 안정적, 자료 많음, 신규가입 크레딧 프로모션 |
| AWS Lightsail | 2 vCPU / 2GB, 월 $24 | 3개월 무료, 이후 상대적으로 비쌈 |
| Google Cloud e2-micro | 공유 2 vCPU / 1GB | 영구 무료지만 미국 리전만 지원(레이턴시 큼) |
