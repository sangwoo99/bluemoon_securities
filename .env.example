# ── Oracle Database (Free) ──────────────────
ORACLE_PASSWORD=change-me
ORACLE_APP_USER=bluemoon
ORACLE_APP_PASSWORD=change-me
ORACLE_PORT=1521

# ── Redis ───────────────────────────────────
REDIS_PORT=6379

# ── Backend (Spring Boot) ──────────────────
SERVER_PORT=8080
JWT_SECRET=change-me-to-a-long-random-string
JWT_ACCESS_EXPIRATION_MS=3600000
JWT_REFRESH_EXPIRATION_MS=1209600000
KIS_APP_KEY=
KIS_APP_SECRET=
KIS_ACCOUNT_NO=
KIS_BASE_URL=https://openapivts.koreainvestment.com:29443

# ── AI 인사이트 (Spring 내부 배치에서 직접 호출 — archive/rag-service 참고) ──
# 뉴스 소스는 NewsData.io(무료 200크레딧/일, 개인/상업적 이용 약관에 명시적으로 허용) — 네이버 검색 API는
# 신규 발급 중단 + AI 활용 금지 약관으로 미사용. https://newsdata.io/register 에서 키 발급
NEWSDATA_API_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ── Frontend (Next.js) ─────────────────────
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
